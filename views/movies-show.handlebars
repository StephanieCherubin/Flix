<!-- Movies-show -->
<!-- This is the page we see when you click on a single movie -->

<div class="row">
    <div class="text-right">
        <a href="/movies/{{movie.id}}/reviews/new" class="btn btn-primary">Write Review</a>
    </div>
    <div class="col-sm-4">
        <img class="img-responsive" src="https://image.tmdb.org/t/p/w500{{movie.poster_path}}"/>
    </div>
    <div class="col-sm-8">
        <h1>{{movie.title}}</h1>
        <p class="lead">{{movie.overview}}</p>
    </div>
</div>

<div class="embed-responsive embed-responsive-21by9">
  <iframe class="embed-responsive-item" src="https://www.youtube.com/embed/{{movie.trailer_youtube_id}}?rel=0"></iframe>
</div>

<!-- Under the movie we see a list of reviews -->
<!-- This is there we pass in the review partial -->
<h3>Reviews</h3>

{{#each reviews}}
  {{> review}}
{{/each}}